---
description: Show the current Jetpack swarm status including task counts, agent states, and system health
disable-model-invocation: true
---

Run the following command and display the output:

```bash
pnpm jetpack status --dir "${JETPACK_WORK_DIR:-.}"
```
